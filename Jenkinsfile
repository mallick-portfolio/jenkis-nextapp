pipeline {
    agent any

    stages {
        stage('Hello') {
            steps {
                git branch: 'main', url: 'https://github.com/mallick-portfolio/jenkis-nextapp.git'
                echo 'Hello World'
            }
        }
    }
}
